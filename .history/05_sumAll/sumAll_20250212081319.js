const sumAll = function(num, num1) {
   let num = Math.min(min, max);
   let num1 = Math.max(min, max);

    sum = 0;

    for(let = min; i <= max; i++) {
        sum += i;
    }


   return sum;
};

// Do not edit below this line
module.exports = sumAll;
